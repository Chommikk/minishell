#include "commands.h"
#include "../minishell.h"
#include "../libft/libft.h"

int	ft_export(t_data *data, char *arg)
{
	size_t	i;
	char	**new;
	char	*var;

	var = ft_substr(arg, 0, ft_strchr(arg, '='));

	i = 0;
	ft_unset(&data->env, var);
	ft_append_arr_str(&data->env, str);

	
}
